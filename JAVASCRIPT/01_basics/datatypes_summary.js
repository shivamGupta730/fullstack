//  Primitive 

// 7 types: number, string, boolean, null, undefined, symbol, bigint

// javascript is dynamically typed, so you don't need to declare the type of a variable before using it.

// reference(Non primitive)
// object, array, function

const heroes=["A","B"]

let myObj={
    name:"John",
    age:30

}

const myFunction=function(){
    console.log("Hello")
}
// to find type of datatypes
console.log(typeof heroes); // object
